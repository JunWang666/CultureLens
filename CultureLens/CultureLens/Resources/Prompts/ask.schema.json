{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "answer": {
      "type": "string",
      "description": "针对用户问题的回答，严格基于知识库片段。"
    },
    "citations": {
      "type": "array",
      "description": "支撑回答的知识库原文摘录。",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "key": {
            "type": "string",
            "description": "本请求内的数字短编号（与输入 knowledge_fragments / neighbors 的 key 一致），不是 UUID。"
          },
          "name": { "type": "string" },
          "fragment": { "type": "string" }
        },
        "required": ["key", "name", "fragment"]
      }
    }
  },
  "required": ["answer", "citations"]
}
