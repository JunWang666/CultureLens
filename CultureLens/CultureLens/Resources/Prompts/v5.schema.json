{
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "attraction_key": {
      "type": "string",
      "description": "当画面目标明确是 nearby_contexts 中的景点时返回对应 attraction_key，否则返回空字符串。"
    },
    "cultural_element_key": {
      "type": "string",
      "description": "命中本次文化内容候选时返回其原始 key；未收录对象返回空字符串。"
    },
    "canonical_name": {
      "type": "string",
      "description": "主候选的规范中文名称；无法识别时为“其他”。"
    },
    "category": {
      "type": "string",
      "enum": ["建筑构件", "器物", "纹样", "展品", "空间", "其他"]
    },
    "confidence": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "当前可见证据对主候选的支持程度。"
    },
    "summary": {
      "type": "string",
      "description": "一句谨慎的对象说明；内容库命中时服务端会使用审核介绍覆盖。"
    },
    "rationale": {
      "type": "string",
      "description": "只写支持主候选的具体可见特征。"
    },
    "uncertainty": {
      "type": "string",
      "description": "仍缺少的证据和建议补拍内容。"
    },
    "time_period": {
      "type": "string",
      "description": "仅在视觉证据足够时填写，否则为空字符串。"
    },
    "region": {
      "type": "string",
      "description": "仅在视觉证据足够时填写，否则为空字符串。"
    },
    "alternatives": {
      "type": "array",
      "minItems": 1,
      "maxItems": 3,
      "description": "按最终可能性从高到低排列的互斥候选，不包含主候选的同义词。",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "cultural_element_key": {
            "type": "string",
            "description": "命中本次文化内容候选时返回其原始 key；未收录对象返回空字符串。"
          },
          "canonical_name": {
            "type": "string"
          },
          "category": {
            "type": "string",
            "enum": ["建筑构件", "器物", "纹样", "展品", "空间", "其他"]
          },
          "confidence": {
            "type": "number",
            "minimum": 0,
            "maximum": 1
          },
          "rationale": {
            "type": "string",
            "description": "该候选与主候选的关键可见区分特征。"
          }
        },
        "required": [
          "cultural_element_key",
          "canonical_name",
          "category",
          "confidence",
          "rationale"
        ]
      }
    }
  },
  "required": [
    "attraction_key",
    "cultural_element_key",
    "canonical_name",
    "category",
    "confidence",
    "summary",
    "rationale",
    "uncertainty",
    "time_period",
    "region",
    "alternatives"
  ]
}
